## Bugs
- [ ] In Audio selection drop down in settings, we should have "SYSTEM SETTING" as the default option, and it should be selected by default. IF user changes, the change should be saved and applied, and persist across sessions. IF the device is not available, it should fall back to the default.
- [ ] For the Conversations and Canvas windows, and even the text input, if you try to drag them or resize them the wrong way, they disappear! And have to be brought back up.
- [ ] Chart Render Error:
    ```
    Invalid chart JSON: Unexpected token 'C', "[CDATA[Serv"... is not valid JSON (the body had extra text around the JSON; the slice we tried was: [CDATA[Service,Avg Hourly Rate,Project Duration,Client Type Prompt Engineering,$80-150,1-4 weeks,Startups, agencies AI Integration,$120-200,2-8 weeks,SaaS platforms Custom Model Training,$150-300+,4-…)
    ```
- [ ] Chart Render Error:
    ```
    Diagram render failed: No diagram type detected matching given configuration for text: <![CDATA[flowchart TD A[Your Local AI Rig] --> B{How many GPUs?} B -->|1-2 cards| C[RunPod - easiest onboarding, USD payouts] B -->|3+ cards| D[Vast.ai - most job volume] B -->|4+ identical| E[io.net - cluster jobs] C --> F{Electricity rate?} D --> F E --> F F -->|Under $0.12/kWh| G[Run it 24/7] F -->|Over $0.15/kWh| H[Only run during peak demand or skip it] G --> I[Earn $5-15/day per consumer GPU] H --> J[Use rig for freelance work instead]]>
    ```
- [ ] Chart Render Error:
    ```
    Invalid chart JSON: Unexpected token 'C', "[CDATA[Plat"... is not valid JSON (the body had extra text around the JSON; the slice we tried was: [CDATA[Platform,Host Share,Gross/Day,Net After Power RunPod,93%,$48.03,$46.35 Vast.ai,85%,$23.95,$22.27 io.net,80%,$32.10,$30.42]])
    ```

## Feature Improvements
- [ ] TTS works fine, but would be nice to have a much better model that handles nuance and emotion better. Perhaps we KEEP what we have, but allow for user to SWITCH to a different TTS model altogether (kokoro-tts, which we can install as a dependency during setup and can download model as needed?)
- [ ] Conversations modal should have button to open chat input (the CTRL + SPACE shortcut)
- [ ] Sometimes reading bullet points and other formatted text gets read wrong, or has no pauses etc. Make sure that we are not stripping out essential formatting that would help with proper pronunciation and pacing. I know we strip asterisks for markdown, but maybe we should keep some formatting?
- [ ] Push notifications for when app is not in the foreground and a message has completed, use Electron's built-in API etc.
- [ ] For installation script, we should install ALL required runtimes as needed and check first for compatible ones already on system. Should use .venv and pnpm and other stuff appropriately.


## UI Improvements
- [ ] For the Prompt Input search bar that appear when you click CTRL-SPACE, it would be cool if the border was a neat rainbow gradient that cycled colors and looked super slick.
- [ ] When hovering over Avatar, there should be a nice background with with opacity. BG should be transparent generally.
- [ ] In conversations UI view, in the chats window, we should have tool calls be hidden in a collapsible section, and only show when user clicks on it. Other things like artifacts etc can stay as is, and the tools can remain to look as is when expanded, just with a collapse button etc.
- [ ] For invalid Charts or render errors, would be great to have the A.I. try to fix the chart or render error, if can't be fixed log error and show a nice error message etc with diag details in the canvas view for that artifact.


## UX Improvements
- [ ] App pop ups should never take away focus from user EXCEPT for when user does CTRL + SPACE to summon input bar or opening things like "conversations" or "canvas" etc.. For Chats that initate a canvas, canvas SHOULD NOT take focus, it should open in foreground on top of all windows, but should not take mouse or text focus at all.
- [ ] Response box should stay open for about 20 seconds after the agent finishes speaking, then automatically close. IF user interacts with the text box, it should remain open. There should be a small indicator in the top of the box that shows the remaining time, it can be a progress bar that fills up as the time decreases.
- [ ] Response box should also have an icon to manually close it, and an icon to open the conversations modal
- [ ] Artifacts modal should only show artifacts for CURRENT conversation
- [ ] Need to make app NOT need Docker, but keep Docker backend as option (especially for quick-start setup WITH HermesAgent setup too) 
- [ ] When submitting a question to the bot, would be great if avatar comes to foreground if not already there. Can configure whether this happens or not in settings. default is to have this enabled.
- [ ] When model is working, just like how we show status on avatar etc, we should show same stuff on the Conversations view as well.
- [ ] Onboarding flow should clearly ask if you need Hermes setup on user's behalf, or if they have their own Hermes agent they wanna point to. Should have a setup doc for getting HermesAgent and gateway going, or point to offical HermesAgent docs etc.
- [ ] Need to make install of app easy via terminal command: Curl or similar for Windows, Mac, Linux; this oneliner should download and run our installer script, which should handle installing EVERYTHING the user needs. Docker should be optional. IF user wants to use Hermes agent via Docker, they can, but need to configure that themselves. Otherwise, Hermes is just installed on the local machine if Hermes installation is selected. We can HAVE a Docker option for the Hermes agent, but it should be optional, and we should research the existing workflows HermesAgent offers for using Docker -- if it can set all that upitself and we just do some preflight checks, that seems best. Generally, to run our app and make it live so the keyboard shotcuts works etc, two things need to happen. ONE: electron app needs to run, TWO: the background APIs currently hosted as a Docker container (and should just be ran directly on system) need to be running so Electron can talk to it. Background APIs now only really host TTS/STT models.
- [ ] We should only use HermesAgent for summarization, we should default to that and HIDE the option to switch to LiteRT for now. LiteRT should not be used and model should not be downloaded. Code should not be removed though, just disabled and not utilized.
- [ ] The conversations window and canvas window should all have rainbow colored borders and high shadow, so they are easy to spot across multiple monitors etc.
- [ ] CTRL + SPACE where you hit SPACE a total of THREE times should bring all four possible windows into view, in center of screen stacked as such: Avatar at top left, Artifacts windows in top right, Conversations in bottom middle, input for typing prompt center center. This is essentially a quick and easy way to bring the entire app to your view. The space bar should be hit three times, with max interval between space bar hits being 1 second. There should be another simple key combo assigned to mke this happen too, CTRL plus something else.
- [ ] BIG - Would be great if the puppet can communicate to you without being prompted. This would be for anything HermesAgent can send on it's own (like a cron job, or whatever else HermesAgent offers). Basically, I can be working, and Hermes sends me a message, and I get a push notification, which opens to the message in Conversations panel/modal. By default, messages can be silent and TTS does not read, but can be enabled if desired. Messages are summarized for TTS and sent in original form for conversations modal, same as normal prompted convos go. Would need to research what HermesAgent offers for this kind of 
- [ ] Production installation script should use a built for production electron app for GUI etc and run the backend code without docker directly on machine as described in other item.

## Things to Confirm
- [ ] Ensure app can be setup by simply inputting the Hermes gateway URL and API key. WE SHOULD not need the config.YAML or anything like that at all. If we do, please write up why and document it.

## Other To-Do (Manual Todo for Human):

- [ ] Need to make THREE nice and proper avatars; hide test ones
- [ ] Make proper documentation
- [ ] Test on Windows + Linux
- [ ] Setup macOS signing cert and notarization workflow via SPT credentials (Sweet Papa Technologies LLC)
 
